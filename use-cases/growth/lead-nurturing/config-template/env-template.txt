# Only true secrets belong here. DB IDs, page IDs, and delivery config
# are saved to agent memory during the SETUP phase and read from there.

# Notion
NOTION_TOKEN=your_n...oken

# Telegram
TELEGRAM_BOT_TOKEN=your_t...oken

# Email (for bulk outreach)
# If using Gmail via Himalaya, configure ~/.config/himalaya/config.toml
EMAIL_FROM=your_sender_email
EMAIL_ACCOUNT=your_himalaya_account_name
