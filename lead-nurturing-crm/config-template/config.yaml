# Hermes Config Template — Litner Chain CIM
# Fill in all values, then copy to ~/.hermes/config.yaml

platforms:
  - type: telegram
    token: "YOUR_TELEGRAM_BOT_TOKEN"
    extra:
      require_mention: true    # In groups, only respond when @mentioned

# Daily report cron — set up after configuring Telegram
# hermes cron create --schedule "0 1 * * *" --name "daily-crm-report" \
#   --prompt "Generate the daily CRM follow-up report and send to Telegram"
# Deliver to: telegram:-100XXXXXXXXX:THREAD_ID
